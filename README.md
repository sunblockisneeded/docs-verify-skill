# docs-verify-skill
LLMs are known to be strong at understanding but weaker at expression. This skill uses fresh agents as crash-test dummies to detect failures in information transfer and iteratively improve the document.
